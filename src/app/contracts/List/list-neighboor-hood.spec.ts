import { ListNeighboorHood } from './list-neighboor-hood';

describe('ListNeighboorHood', () => {
  it('should create an instance', () => {
    expect(new ListNeighboorHood()).toBeTruthy();
  });
});
