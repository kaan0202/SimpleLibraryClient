import { UpdateDirective } from './update.directive';

describe('UpdateDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateDirective();
    expect(directive).toBeTruthy();
  });
});
